//! `.txt` sidecar metadata file parsing.
//!
//! Some generators (e.g. FooocusMRE) write generation parameters to a sibling
//! `.txt` file. The parser is implemented in **M2 (Scanning & Indexing)**.
